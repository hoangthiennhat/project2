mixin title(name,check= false)
    .title.d-flex.justify-content-between.align-items-center
        span.text-capitalize= name
        if check
            .arrow.d-flex
                .arrow-left
                .arrow-right